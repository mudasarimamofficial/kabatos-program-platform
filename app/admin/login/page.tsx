'use client'
import { useState } from 'react'
import Link from 'next/link'
export default function Page() { const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); return <main className="login-page"><div className="login-card"><div className="eyebrow">COMPREX ADMIN</div><h1>Sign in to continue.</h1><p>Manage brands, programs, and customer access.</p><form className="form-stack" onSubmit={(event) => { event.preventDefault(); window.location.href = '/admin' }}><label>Email<input type="email" value={email} onChange={(event) => setEmail(event.target.value)} required /></label><label>Password<input type="password" value={password} onChange={(event) => setPassword(event.target.value)} required /></label><button className="btn btn-dark" type="submit">Sign in</button></form><Link className="text-link" href="/comprex">Return to customer preview</Link></div></main> }
